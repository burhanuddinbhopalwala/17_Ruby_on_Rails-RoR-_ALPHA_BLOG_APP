<h2 align = "center">Welcome <%= @user.username %></h2>
<div class = "container">
    <div class = "row">
        <div class="center col-xs-offset-4 col-sm-offset-4 col-md-offset-4 
        col-lg-offset-4 col-xs-4 col-sm-4 col-md-4 col-lg-4">
            <%= gravatar_for(@user, size: 150) %>
            <h4 align = "center"><%= @user.username %></h4>
        </div>
    </div>
</div>
<h4 align = "center"><%= @user.username %>'s articles</h4>
<div align = "center"><%= will_paginate(@user_articles) %></div>
<%= render("articles/article", articles_obj: @user_articles) %>
<div align = "center"><%= will_paginate(@user_articles) %></div>
