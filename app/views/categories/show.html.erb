<h1 align = "center"><%= "Category: #{@category.name}" %></h1> 
<div align = "center">
    <% if(logged_in?() and current_user.admin?()) %>
        <div class = "row">
            <span class = "badge"><%= link_to("Edit category name", 
            edit_category_path(@category)) %></span>
        </div>
    <% end %>
    <%= will_paginate(@category_articles) %>
</div>
<%= render("articles/article", articles_obj: @category_articles) %>
<div align = "center"><%= will_paginate(@category_articles) %></div>
<!-- IMP: Need to paginate @categories.articles hence passing that 
only from the controller as @categories_articles -->